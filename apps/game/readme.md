# @open-pixel/game

RPG-JS game package for Open Pixel: the village map, resource actions (farm, chop, mine, craft, prepare), order fulfillment, and the completion loop that issues the Game Completion Receipt.

Run from the repo root:

```bash
npm run dev:game
```
