# @open-pixel/web

React + Vite frontend for Open Pixel: landing page, guest badge claim flow, optional wallet proof (`personal_sign` only), and the Supabase-backed leaderboard.

Run from the repo root:

```bash
npm run dev:web
```

Requires the env vars documented in the root [`.env.example`](../../.env.example).
